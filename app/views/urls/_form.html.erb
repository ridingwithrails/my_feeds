<%= form_for(@url, :html => { :class => "well"}) do |f| %>
  <% if @url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@url.errors.count, "error") %> prohibited this url from being saved:</h2>

      <ul>
      <% @url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="actions">
 	<%= f.label :url %>	 
	<%= f.text_field :url %>
 	<%= f.label :topic %>
	<%= f.text_field :topic %>
	<%= f.label :notes %>
	<%= f.text_area :notes, :rows => 6 %>
    <div class="formactions"><%= f.submit :class => "btn-primary" %></div>
  </div>
<% end %>
